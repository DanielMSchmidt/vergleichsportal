-content_for :head do 
  %h3 Admin Dashboard

%ul.accordion
  -if right_permission?("lock_user")
    %li.active
      = render :partial => "home/admin/user"

  -if right_permission?("enable_provider")
    %li#provider
      = render :partial => "home/admin/provider"

  -if right_permission?("modify_ad")
    %li
      .title
        %h5 Werbung
      .content
        %h6 Aktiv
        %hr
        #active-ad
        
          = render @active_advertisments

        %h6 Inactive
        %hr
        #ad-list

          = render @inactive_advertisments
        .row
          .six.columns.centered
            %a.button.expand{"data-reveal-id" => "add-advertisment-modal", :href => "#"} Werbung hinzufügen
    - content_for :modal do 
      = render :partial => "home/admin/add_advertisment"

  -if right_permission?("view_statistic")
    %li
      = render :partial => "home/admin/statistic"
