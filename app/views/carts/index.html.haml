%h1 Listing carts

%table
  %tr
    %th User
    %th
    %th
    %th

  - @carts.each do |cart|
    %tr
      %td= cart.user_id
      %td
        = link_to 'Use', use_cart_path(cart) unless cart == @active_cart
      %td= link_to 'Destroy', cart, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Cart', new_cart_path
